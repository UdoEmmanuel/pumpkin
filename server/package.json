{
  "name": "pumpkin-server",
  "version": "0.1.0",
  "private": true,
  "description": "Self-hosted chat backend for Pumpkin — replaces Firestore for chats/messages only. Firebase Auth + the Firestore users collection stay as-is (see README).",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "firebase-admin": "^12.3.1",
    "mongoose": "^8.5.2",
    "node-cron": "^3.0.3",
    "socket.io": "^4.7.5"
  }
}
