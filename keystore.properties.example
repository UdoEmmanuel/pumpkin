# Copy this file to keystore.properties (same folder, project root) and fill
# in real values. keystore.properties is gitignored — never commit it or the
# .jks file it points to.
storeFile=../pumpkin-release.jks
storePassword=changeme
keyAlias=pumpkin
keyPassword=changeme
