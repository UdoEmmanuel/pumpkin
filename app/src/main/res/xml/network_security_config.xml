<?xml version="1.0" encoding="utf-8"?>
<!-- PRD 9: standard TLS in transit is assumed sufficient for this school project
     (E2E encryption is a stretch goal, not built in Phase 1). Cleartext is
     forbidden everywhere EXCEPT the two loopback aliases used to reach a
     locally-running dev copy of server/ from the emulator/device — see
     ServerConfig.kt. Point BASE_URL at a real https:// deployment before
     distributing a build outside local development. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
